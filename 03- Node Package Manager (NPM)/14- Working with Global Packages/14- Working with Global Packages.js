// Global Packages: Packages not tied to any specific project.

// Example: npm is a global package. It's a command-line tool available from any folder.

// Install a global package:
// npm install -g <package-name>

// Uninstall a global package:
// npm uninstall -g <package-name>
